# Common values shared for all charts
release_name: datahub-local-workflows

# Sidecar options applied as ConfigMap annotations, keyed by bundle name
# (the .zip basename under files/). Supported keys: tags, certified-by, publish.
superset_dashboards:
  bodega:
    tags: "bodega"
    certified-by: "datahub-local-workflows"
