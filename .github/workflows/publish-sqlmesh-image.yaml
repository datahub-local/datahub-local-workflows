name: Publish SQLMesh Image

on:
  workflow_dispatch:

  push:
    branches: [main]
    paths:
      - workflows/sqlmesh/**
  pull_request:
    paths:
      - workflows/sqlmesh/**

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Python with uv
        working-directory: workflows/sqlmesh
        run: uv python install "$(cat .python-version)"

      - name: Install dependencies
        working-directory: workflows/sqlmesh
        run: uv sync --extra dev

      - name: Validate SQLMesh projects (dry-run plan)
        working-directory: workflows/sqlmesh
        run: |
          source pipelines/example_db/.env.local
          uv run sqlmesh --project example_db plan --no-prompts --auto-apply --skip-tests 2>&1 | head -50 || true
          uv run sqlmesh --project pi plan --no-prompts --auto-apply --skip-tests 2>&1 | head -50 || true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: workflows/sqlmesh
          push: ${{ github.ref == 'refs/heads/main' }}
          platforms: linux/amd64,linux/arm64
          tags: ghcr.io/datahub-local/datahub-local-workflows-sqlmesh:main
          cache-from: type=gha
          cache-to: type=gha,mode=max
