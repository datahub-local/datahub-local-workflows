name: Publish dbt Image

on:
  workflow_dispatch:

  push:
    branches: [main]
    paths:
      - workflows/dbt/**
  pull_request:
    paths:
      - workflows/dbt/**

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Python with uv
        working-directory: workflows/dbt
        run: uv python install "$(cat .python-version)"

      - name: Install dependencies
        working-directory: workflows/dbt
        run: uv sync --extra dev

      - name: Validate dbt projects (parse on DuckDB local target)
        working-directory: workflows/dbt
        run: |
          uv run dbt parse --project-dir pi --profiles-dir pi --target local
          uv run dbt parse --project-dir example_db --profiles-dir example_db --target local

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: workflows/dbt
          push: ${{ github.ref == 'refs/heads/main' }}
          platforms: linux/amd64,linux/arm64
          tags: ghcr.io/datahub-local/datahub-local-workflows-dbt:main
          cache-from: type=gha
          cache-to: type=gha,mode=max
